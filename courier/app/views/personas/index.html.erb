<h1><%= t('editarpersona') %></h1>

<table width="80%">
  <tr>
    <th>Email</th>
    <th><%= t('nombrez') %></th>
    <th><%= t('apellido') %></th>
    <th><%= t('fechanac') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @personas.each do |persona| %>
  <tr align="Center">
    <td><%= persona.email %></td>
    <td><%= persona.nombre %></td>
    <td><%= persona.apellido %></td>
    <td><%= persona.fNacimiento %></td>
    <td><%= link_to t('mostar'), persona %></td>
    <td><%= link_to t('editar'), edit_persona_path(persona) %></td>
    <td><%= link_to t('eliminar'), persona, :confirm => t('eliminarseguro'), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('nuevapersona'), new_persona_path %>
