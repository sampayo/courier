<%= form_for(@empleado) do |f| %>
  <% if @empleado.errors.any? %>
    <!-- <div id="error_explanation">
      <h2><%= pluralize(@empleado.errors.count, "error") %> prohibited this empleado from being saved:</h2>

      <ul>
      <% @empleado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div> -->
  <% end %>
   <div class="field">
    <%= f.label t('empleado') %><br />
    <%= collection_select(:empleado,:personas_id,@query,:id,:nombre) %>
  </div> 
  <div class="field">
    <%= f.label t('cargo') %><br />
    <%= f.text_field :cargo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
