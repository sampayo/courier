<h1>Listado de mis direcciones</h1>

<table width="80%" align="Center">
  <tr>
    <th>Nombre</th>  	
  	<th>Pa&iacute;s</th>
  	<th>Ciudad</th>
  	<th>Avenida</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @direccions.each do |direccion| %>
  <tr align="Center">
    <td><%= direccion.nombre %></td>  	
  	<td><%= direccion.pais %></td>
  	<td><%= direccion.ciudad %></td>
    <td><%= direccion.avCalle %></td>
    <td><%= link_to 'Mostar', direccion %></td>
    <td><%= link_to 'Editar', edit_direccion_path(direccion) %></td>
    <td><%= link_to 'Borrar', direccion, :confirm => 'Esta seguro que desea Borrarla?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Agregar Direccion', new_direccion_path %>
